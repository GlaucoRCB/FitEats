<div class="card">
    <div class="card-body">
        <h1 class="">{{receita.titulo}}</h1>
        <hr>
        <small class="">Data de publicação: {{receita.data}}</small><br>
        <hr>
        <p class="">{{receita.modopreparo}}</p>
    </div>
</div>
